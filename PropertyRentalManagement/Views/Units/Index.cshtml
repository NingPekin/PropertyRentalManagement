@model IEnumerable<PropertyRentalManagement.Models.Unit>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UnitNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Building.BuildingId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UnitNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Building.BuildingId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UnitId }) |
            @Html.ActionLink("Details", "Details", new { id=item.UnitId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.UnitId })
        </td>
    </tr>
}

</table>
